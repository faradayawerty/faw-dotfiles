#! /bin/bash

touch ~/.vimrc
touch ~/.bashrc
touch ~/.tmux.conf

echo "so $(realpath vimrc)" >> ~/.vimrc
echo "source $(realpath bashrc)" >> ~/.bashrc
echo "source-file $(realpath tmux.conf)" >> ~/.tmux.conf

