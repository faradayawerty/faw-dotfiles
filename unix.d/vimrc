
call plug#begin('~/.vim/plugged')
	Plug 'christoomey/vim-system-copy'
	Plug 'mkitt/tabline.vim'
	Plug 'fxn/vim-monochrome'
call plug#end()

set number
set tabstop=6
set shiftwidth=6
set noswapfile 
set hlsearch
set incsearch
set ttimeout
set ttimeoutlen=10
set listchars=tab:>-,space:.,eol:~

syntax on
colorscheme monochrome

highlight TabLine ctermbg=none ctermfg=none cterm=none
highlight TabLineFill ctermfg=gray ctermbg=none cterm=none
highlight TabLineSel ctermfg=white ctermbg=none cterm=bold
highlight LineNr ctermbg=none ctermfg=blue cterm=bold
highlight ColorColumn ctermbg=red
highlight Visual ctermfg=black ctermbg=blue cterm=none

noremap ,S :set spell! spelllang=en,ru<CR>
noremap ,w :set list!<cr>
noremap ,p :set paste!<cr>
noremap ,c :execute "set colorcolumn=" . (&colorcolumn == "" ? "80" : "")<cr>
noremap <C-l> :noh<cr>

let g:system_copy_silent = 0

if executable('xclip')
	let g:system_copy#copy_command='xclip -sel clipboard'
	let g:system_copy#paste_command='xclip -sel clipboard -o'
endif

if has("win32") || has("win16")
	let g:system_copy#copy_command='clip'
	let g:system_copy#paste_command='paste'
endif

" return to the same line you left off at
augroup line_return
	au!
	au BufReadPost *
		\ if line("'\"") > 0 && line("'\"") <= line("$") |
		\	execute 'normal! g`"zvzz' |
		\ endif
augroup END


