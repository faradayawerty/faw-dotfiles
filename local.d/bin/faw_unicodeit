#!/bin/python

import unicodeit
import os
import subprocess
from time import sleep

string = subprocess.check_output(['xsel']).decode('utf-8')
sleep(0.1)
os.system('xdotool type ' + unicodeit.replace(string).replace('(', '\(').replace(')', '\)'))

