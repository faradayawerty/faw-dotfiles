#!/bin/bash

[ -z "$SCREENSHOT_COMMAND" ] && SCREENSHOT_COMMAND="maim $@"
[ -z "$IMAGE_VIEWER" ] && IMAGE_VIEWER="sxiv"

dir=~/Pictures/screenshots
mkdir -p $dir
cd $dir

name=screenshot$(date +"%d-%m-%H-%M-%S").png
$SCREENSHOT_COMMAND $name && $IMAGE_VIEWER $(ls -t)

