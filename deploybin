#! /bin/bash

mkdir -p ~/.local/bin
mkdir -p ~/.local/share

for i in $(ls bin)
do
	[ "$i" = "share" ] && continue
	ln -snf $(realpath bin/$i) ~/.local/bin/$i
done

for i in $(ls bin/share)
do
	ln -snf $(realpath bin/share/$i) ~/.local/share/$i
done

