#!/bin/bash

mkdir -p ~/.config/kak
ln -snf $(realpath kakrc) ~/.config/kak/kakrc

mkdir -p ~/.config/kak/autoload
cp -r autoload/* ~/.config/kak/autoload/

mkdir -p ~/.config/kak-lsp
ln -snf $(realpath kak-lsp.toml) ~/.config/kak-lsp/kak-lsp.toml


