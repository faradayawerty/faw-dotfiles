#!/bin/bash

mkdir -p ~/.config/kak
ln -snf $(realpath kakoune/kakrc) ~/.config/kak/kakrc

mkdir -p ~/.config/kak/autoload
cp -r kakoune/autoload/* ~/.config/kak/autoload/

mkdir -p ~/.config/kak-lsp
ln -snf $(realpath kakoune/kak-lsp.toml) ~/.config/kak-lsp/kak-lsp.toml


