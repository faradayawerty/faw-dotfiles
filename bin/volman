#!/bin/bash

# simple volume manager

# usage:
# volman up - increase volume
# volman down - decrease volume
# volman mute - mute volume

arg="$@"

[ "$arg" = "up" ] && amixer set Master 5%+
[ "$arg" = "down" ] && amixer set Master 5%-
[ "$arg" = "mute" ] && amixer -q set Master toggle


