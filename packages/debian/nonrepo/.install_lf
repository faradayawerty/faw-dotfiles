#!/bin/bash
[ -x "$(command -v lf)" ] && echo lf already installed && exit
sudo apt install -t buster-backports libncurses-dev
env CGO_ENABLED=0 GO111MODULE=on go get -u -ldflags="-s -w" github.com/gokcehan/lf

