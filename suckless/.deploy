#!/bin/bash

for i in $(ls); do
	cd $i/src
	sudo make clean && make && sudo make install
	cd ../..
done

mkdir -p ~/.config/sxiv/exec
ln -snf $(realpath sxiv/cfg/key-handler) ~/.config/sxiv/exec/key-handler

